const fs=require('fs'),path=require('path');
const root=path.resolve(process.argv[2]||'candidate');
const nativeFile=path.join(root,'src/main/persistence/documents/sqliteNativeDocumentCommands.ts');
const ipcFile=path.join(root,'src/main/ipc/ipcRegistry.ts');
const preloadFile=path.join(root,'src/preload/orbitaApi.ts');
for(const file of [nativeFile,ipcFile,preloadFile])if(!fs.existsSync(file))throw new Error('required owner missing '+path.relative(root,file));
const native=fs.readFileSync(nativeFile,'utf8'),ipc=fs.readFileSync(ipcFile,'utf8'),preload=fs.readFileSync(preloadFile,'utf8');
function uniq(a){return [...new Set(a)].sort();}
function balanced(text,start,open='(',close=')'){let depth=0,quote=null;for(let i=start;i<text.length;i++){const ch=text[i];if(quote){if(ch===quote&&text[i-1]!=='\\')quote=null;continue;}if(ch==='"'||ch==="'"||ch==='`'){quote=ch;continue;}if(ch===open)depth++;else if(ch===close){depth--;if(depth===0)return {body:text.slice(start+1,i),end:i};}}return null;}
function splitParams(raw){const out=[];let cur='',depth=0,quote=null;for(let i=0;i<raw.length;i++){const ch=raw[i];if(quote){cur+=ch;if(ch===quote&&raw[i-1]!=='\\')quote=null;continue;}if(ch==='"'||ch==="'"||ch==='`'){quote=ch;cur+=ch;continue;}if('(<[{'.includes(ch))depth++;if(')>]}'.includes(ch))depth--;if(ch===','&&depth===0){out.push(cur.trim());cur='';}else cur+=ch;}if(cur.trim())out.push(cur.trim());return out;}
function fieldFacts(text){return [...text.matchAll(/(?:^|[,{;\n])\s*([A-Za-z_$][\w$]*)(\?)?\s*:\s*([^,;}\n]+)/g)].map(m=>({name:m[1],optional:m[2]==='?',typeKind:/string/.test(m[3])?'string':/boolean/.test(m[3])?'boolean':/number/.test(m[3])?'number':/null|undefined/.test(m[3])?'nullable-or-undefined':'other'}));}
const marker='function importNativeDocumentToWorkInDatabase';let idx=native.indexOf(marker);if(idx<0)idx=native.indexOf('importNativeDocumentToWorkInDatabase');if(idx<0)throw new Error('importNativeDocumentToWorkInDatabase signature missing');const open=native.indexOf('(',idx);const sig=balanced(native,open);if(!sig)throw new Error('balanced import signature unresolved');const params=splitParams(sig.body);if(params.length<3)throw new Error(`expected database request options parameters, got ${params.length}`);const optionsText=params[2];let optionsFields=fieldFacts(optionsText);const optionTypeIds=uniq([...optionsText.matchAll(/\b[A-Z][A-Za-z0-9_$]*\b/g)].map(m=>m[0]).filter(x=>x!=='DatabaseSync'));
if(!optionsFields.length&&optionTypeIds.length){for(const typeName of optionTypeIds){const esc=typeName.replace(/[.*+?^${}()|[\]\\]/g,'\\$&');for(const re of [new RegExp(`interface\\s+${esc}\\b[^\\{]*\\{`),new RegExp(`type\\s+${esc}\\s*=\\s*\\{`)]){const m=re.exec(native);if(m){const brace=native.indexOf('{',m.index);const block=balanced(native,brace,'{','}');if(block)optionsFields.push(...fieldFacts(block.body));}}}}
const importNames=uniq([...native.matchAll(/import\s+\{([\s\S]*?)\}\s+from\s+['"]([^'"]+)['"]/g)].flatMap(m=>m[1].split(',').map(x=>x.trim().split(/\s+as\s+/)[1]||x.trim()).filter(x=>/document|file|storage|hash|checksum|managed|copy|read|write/i.test(x))));
const nativeFacts={optionsFields:uniq(optionsFields.map(x=>JSON.stringify(x))).map(x=>JSON.parse(x)),optionTypeIds,helperImports:importNames,usesCopyFile:/copyFile|copyFileSync|fs\.copy/i.test(native),usesReadFile:/readFile|readFileSync/i.test(native),usesWriteFile:/writeFile|writeFileSync/i.test(native),usesMkdir:/mkdir|mkdirSync/i.test(native),usesSha256:/sha256|createHash\s*\(\s*['"]sha256/i.test(native),managedRootSignals:uniq([...native.matchAll(/\b([A-Za-z_$][\w$]*(?:Root|Dir|Directory|Path))\b/g)].map(m=>m[1]).filter(x=>/managed|document|storage/i.test(x))).slice(0,40),sourceExistenceValidation:/existsSync|statSync|accessSync|ENOENT|not found|ne postoji/i.test(native),transactionSignal:/transaction|BEGIN|ROLLBACK|COMMIT/i.test(native),testFailHook:/__testOnlyFailAfterDatabaseMutation/.test(native)};
const channel='orbita:pickAndImportNativeDocumentToWork';const channelIndex=ipc.indexOf(channel);if(channelIndex<0)throw new Error('native import IPC channel missing');const windowText=ipc.slice(Math.max(0,channelIndex-5000),Math.min(ipc.length,channelIndex+10000));const ipcFacts={channel,usesShowOpenDialog:/showOpenDialog/.test(windowText),usesShowOpenDialogSync:/showOpenDialogSync/.test(windowText),usesDialog:/\bdialog\b/.test(windowText),checksCanceled:/\.canceled|\.cancelled|canceled\s*:/i.test(windowText),checksEmptyFilePaths:/filePaths[^\n]{0,180}(?:length|\[0\])/.test(windowText),callsRepository:/importNativeDocumentToWorkThroughRepository/.test(windowText),callsRuntime:/importNativeDocumentToWorkRuntime/.test(windowText),cancelReturnKeys:uniq([...windowText.matchAll(/(?:canceled|cancelled|fileName|documentId|workspace|error)\s*:/gi)].map(m=>m[0].replace(/\s*:.*/,''))).slice(0,30),filePickerProperties:uniq([...windowText.matchAll(/\b(?:properties|filters|title|defaultPath)\s*:/g)].map(m=>m[0].replace(/\s*:.*/,'')))};const preloadFacts={channelReferenced:preload.includes(channel),safeInvoke:preload.includes('safeInvoke'),methodNames:uniq([...preload.matchAll(/([A-Za-z_$][\w$]*import[A-Za-z0-9_$]*)\s*:/gi)].map(m=>m[1])).slice(0,40)};
if(!ipcFacts.usesShowOpenDialog&&!ipcFacts.usesShowOpenDialogSync)throw new Error('real native file picker not resolved');if(!nativeFacts.optionsFields.length)throw new Error('native import options fields unresolved');
console.log(JSON.stringify({state:'PASS',audit:'ORBITA_W7B_NATIVE_IMPORT_RUNTIME_CONTRACT_FORENSIC',sourceExposure:'SEMANTIC_FACTS_ONLY_NO_SOURCE_SNIPPETS',nativeOwner:'src/main/persistence/documents/sqliteNativeDocumentCommands.ts',ipcOwner:'src/main/ipc/ipcRegistry.ts',preloadOwner:'src/preload/orbitaApi.ts',nativeFacts,ipcFacts,preloadFacts,verdict:{optionsResolved:true,realPlatformPicker:true,cancelBoundaryInIpc:ipcFacts.checksCanceled||ipcFacts.checksEmptyFilePaths,successWriteOwnerPreserved:true,errorRollbackProofRequired:true,physicalCancelDialogProofRequired:true,noProductMutation:true}},null,2));
