const fs=require('fs');const path=require('path');const root=path.resolve(process.argv[2]||'candidate');const rel='src/renderer/styles/canonical/people-operational-closure.css';const text=fs.readFileSync(path.join(root,rel),'utf8');const known=['people-context-actionbar','people-next-step-panel','people-command-focus','people-single-command-panel','people-detail-tabs'];const rules=[];for(const m of text.matchAll(/([^{}]+)\{([^{}]*)\}/g)){const selector=m[1].trim(),body=m[2];if(!selector.includes('people-context-actionbar')||!/display\s*:\s*none(?:\s*!important)?\s*;?/i.test(body))continue;rules.push({path:rel,classCount:(selector.match(/\./g)||[]).length,attributeCount:(selector.match(/\[/g)||[]).length,known:Object.fromEntries(known.map(k=>[k,selector.includes(k)])),hasDescendantCombinator:/\s/.test(selector),hasChildCombinator:selector.includes('>'),hasPseudo:selector.includes(':')});}console.log(JSON.stringify({state:rules.length?'PASS':'FAIL',rules},null,2));if(!rules.length)process.exit(2);